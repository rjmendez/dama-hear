# DAMA Hear documentation index

The documents in the first section below are the **agreed target architecture** for the
migration of DAMA Hear to a standalone, self-hosted acoustic platform — a system that runs
without AWS, without a shared PVC as an interchange, and without `dama-gotchi` as a
dependency. Phase 0 (contract freeze and baseline) is implemented and merged; Phase 1 and
later phases are in progress. These are design records, not runbooks: where a design and the
current code disagree, the design states the intended end state.

## Migration and target architecture

| Document | What it defines |
|---|---|
| [standalone-migration.md](standalone-migration.md) | The phased, reversible expand/migrate/verify/rollback path from the current AWS + PVC + Redis coupling to a site-owned platform. Start here. |
| [migration-risk-register.md](migration-risk-register.md) | The dated, evidence-backed register of what is currently true: architecture blockers versus operational debts, their owners, exit gates, and which of them gate the next phase. |
| [api-boundaries.md](api-boundaries.md) | The northbound service map and contract conventions: which service owns which durable resource, its API style, and the events it publishes. |
| [repository-structure.md](repository-structure.md) | The target repository layout and the extraction plan: one platform monorepo, `dama-gotchi` kept as a separate consumer application. |
| [deployment.md](deployment.md) | Progressive deployment profiles (single machine/Compose, small HA cluster, multi-site Kubernetes) sharing one image, config schema, and data layout. |
| [fleet-management.md](fleet-management.md) | The standalone fleet control plane: device identity, desired vs reported state, signed firmware compatibility, staged rollouts, and lifecycle audit. |
| [ml-lifecycle.md](ml-lifecycle.md) | The minimal self-hosted acoustic ML lifecycle: corpus and retention, labelling, reproducible training, promotion, edge deployment, and rollback. |
| [phase0-freeze-contracts.v1.md](phase0-freeze-contracts.v1.md) | The Phase 0 contract freeze: the hashed, reproducible inventory of wire profiles, schemas, and firmware build metadata that later phases must not silently break. |
| [adapter-conformance.md](adapter-conformance.md) | The Phase 1 suite every ingress adapter must pass with no live service, and the merge-blocking recoupling import boundary over `hear/` and `modules/`. |
| [durable-postgres-schema.md](durable-postgres-schema.md) | The Phase 2 canonical Postgres schema for the durable heartbeat/event outbox: device-scoped identity, claim leases, partitions and retention, O(1) health, access control, backfill and rollback. Design and DDL only; not deployed. |
| [phase2-postgres-migration-plan.md](phase2-postgres-migration-plan.md) | The Phase 2 execution plan for moving the durable outbox onto that schema: the measured SQLite→Postgres data map, the expand/migrate/verify/contract stages, feature flags, write ordering and dual-write error semantics, backfill watermarks, hash reconciliation, shadow compare, observability, rollback, and the entry/exit gates. Plan only; nothing is provisioned or cut over. |
| [object-store-backend.md](object-store-backend.md) | The Phase 3 object-store backend capability audit: what `hear/objectstore/backend.py` demands of a store, the measured host limits behind it (the ext4 root is a VHDX on a disk with 52 GB left, not the 240 G `df` reports), probed filesystem semantics on both candidate roots, a backend comparison grounded in upstream evidence, the importer gaps that block a live run whichever backend wins, and the choices still owed to an operator. Audit only; no backend is selected, provisioned or configured. |
| [phase3-storage-capacity-model.md](phase3-storage-capacity-model.md) | The Phase 3 storage capacity model for the single-node profile: measured device geometry (the real bound is the 51.4 GiB left on the Windows disk behind the VHDX, not the 263.9 GiB `df` reports), re-measured source growth and the stable ~59 % duplicate fraction, the backup chain over an uncapped versus a retained source, shadow blob/metadata/pointer/staging terms, recovery capacity, and the alert and stop conditions that replace the unsatisfiable 320 GiB G5 reservation with 140 GiB of backup budget on `F:` and a 24 GiB bounded shadow on `/`. Model only; nothing is reserved, provisioned or applied. |
| [phase3-object-encryption-contract.md](phase3-object-encryption-contract.md) | The Phase 3 **production** object encryption and capability contract: an abstract KMS-shaped key authority that never exports a key, one random per-object DEK wrapped under a per-tenant/per-class KEK, a framed AEAD whose AAD binds object key, blob id, tenant, sensitivity class, key reference and schema version, the "no plaintext before authentication" reader rule, dedupe kept at the identity layer so convergent ciphertext leakage ends, thirteen backend capability probes including two that must fail, IAM-enforced delete denial, KEK rotation as re-wrap and revocation as crypto-erasure, the `sealedmeta.v2` schema and its migration, and the twelve gates that must be green first. Design only; no key, no KMS, no dependency, no backend, no cryptography implemented. |
| [decisions/](decisions/) | Numbered, append-only decision records. One per contract or layout decision: what was decided, what evidence forced it, and what it means for forward/rollback/mixed-version behaviour. [0001](decisions/0001-hear-ingest-v1-envelope-and-codec.md) the `hear.ingest.v1` envelope and codec; [0002](decisions/0002-contract-repository-layout.md) the canonical home for published contracts; [0003](decisions/0003-phase2-durable-outbox-postgres-cutover.md) the Phase 2 durable-outbox cut-over (dual-write, backfill, reversal); [0004](decisions/0004-phase4-https-batch-ingest-adapter.md) the Phase 4 additive HTTPS batch ingest adapter; [0005](decisions/0005-phase4-dual-write-observability.md) Phase 4 dual-write observability and reconciliation; [0007](decisions/0007-phase5-shadow-read-comparator.md) the Phase 5 shadow-read comparator; [0008](decisions/0008-device-side-batch-client-spool.md) the device-side batch client and bounded uplink spool; [0009](decisions/0009-phase6-list-read-comparison.md) Phase 6 list, aggregate and export read comparison; [0010](decisions/0010-phase3-object-encryption-and-capability-contract.md) the Phase 3 production object encryption and backend capability contract. |
| [REDESIGN-LESSONS.md](REDESIGN-LESSONS.md) | Postmortem of the existing fleet: observed failure modes and the systems-design lessons that constrain the redesign. Evidence-tagged. |
| [loci-memory-validation.md](loci-memory-validation.md) | Validation of the Loci spatial/memory behaviour used by the localization lane. |

## Decision records

Numbered, dated records of decisions that constrain later phases. A decision record states
what was chosen, the in-tree evidence it rests on, and what was deliberately left open.

| Record | What it decides |
|---|---|
| [0001 - `hear.ingest.v1` envelope and codec](decisions/0001-hear-ingest-v1-envelope-and-codec.md) | The canonical ingest envelope as a codec-agnostic logical schema, UTF-8 JSON as the only normative v1 wire codec, and a closed identity tuple for `event_id`. |
| [0002 - contract repository layout](decisions/0002-contract-repository-layout.md) | The canonical home for a published contract: schema, fixtures, generator, and the decision record that names it. |
| [0003 - Phase 2 durable outbox Postgres cut-over](decisions/0003-phase2-durable-outbox-postgres-cutover.md) | The dual-write, backfill and reversal plan for moving the heartbeat outbox from SQLite to Postgres. |
| [0004 - Phase 4 HTTPS batch ingest adapter](decisions/0004-phase4-https-batch-ingest-adapter.md) | An additive HTTPS batch ingest path: frame versioned separately from items, receipt-based contiguous acknowledgement, identity-based replay safety, and no broker or queue at this scale. |
| [0005 - Phase 4 dual-write observability and reconciliation](decisions/0005-phase4-dual-write-observability.md) | How a future legacy + HTTPS dual-write is observed: recorded join key, a closed classification vocabulary, grace-window `pending`, and redacted mismatch receipts. |
| [0006 - `HEAR_ADMIN_TOKEN` scope, custody and lifecycle](decisions/0006-admin-token-provisioning-policy.md) | Per-node versus fleet-wide admin tokens, authority of record, enrollment injection, rotation/revocation, lost-token recovery, redaction rules, and the tooling changes each option implies. Generates no credential. |
| [phase4-https-batch-ingest.md](phase4-https-batch-ingest.md) | The operational specification behind 0004: API and auth, sizing and limits, retry/offline durability, timestamp and version semantics, refusal visibility, TLS/key lifecycle, observability, dual-write comparison and rollback. |
| [0005 - Phase 4 dual-write observability](decisions/0005-phase4-dual-write-observability.md) | How a future legacy + HTTPS dual-write is correlated, classified and evidenced: a recorded join key, a closed classification vocabulary, grace windows, and redacted mismatch receipts with no auto-repair. |
| [phase4-dual-write-observability.md](phase4-dual-write-observability.md) | The operational specification behind 0005: correlation identity, tolerances, the fifteen-member classification, the staged `hear.reconcile.receipt.v1`, metrics, SLOs, alerts, sampling, the repair-queue boundary, fault injection and rollback. |
| [0007 - Phase 5 shadow-read comparator](decisions/0007-phase5-shadow-read-comparator.md) | How legacy and canonical **read** answers are compared before a read cutover: query fingerprints, settle horizons and snapshot skew, absence decomposed into in-flight/late/retention/tombstone/lost, order and cursor properties separate from content, blind comparison for restricted values, and gate impact separate from repair action. |
| [0008 - device-side batch client and uplink spool](decisions/0008-device-side-batch-client-spool.md) | Where a node may hold an uplink backlog and what bounds it: SD `/spool/` rather than NVS or a new partition, a budget derived from measured free space, a non-authoritative spool whose eviction costs latency rather than data, immutable append-only records, a dual-slot CRC'd acknowledgement watermark, an 8-item batch sized by the receipt rather than the request, fail-closed receipt parsing, and compile-time-off rollout with today's push path as the permanent fallback. Changes no firmware. |
| [phase4-device-batch-spool.md](phase4-device-batch-spool.md) | The operational specification behind 0008: per-board flash/RAM/card constraints, spool layout and record framing, bounded append and oldest-first eviction, identity and ordering across reboots, clock-invalid records that are never repaired, batching/ack/retry/backoff/duplicate/poison handling, power-loss and corruption recovery, the credential and TLS trust boundary, on-node observability, OTA and mixed-version rollout with rollback, and the implementation gates that remain. |
| [phase5-shadow-read-comparator.md](phase5-shadow-read-comparator.md) | The operational specification behind 0007: evidence classes and comparators, query identity, time and pagination semantics, the nineteen-member classification, the staged `hear.shadowread.receipt.v1`, sampling and cost, metrics/SLOs/alerts, the repair boundary, operator evidence bundles, 26 fault-injection cases, rollback and the thirteen read-cutover gates. |
| [0009 - Phase 6 list, aggregate and export read comparison](decisions/0009-phase6-list-read-comparison.md) | How **list, aggregate and export** answers are compared, closing the grain the Phase 5 record comparator deferred: snapshot pinning for a cursorless legacy surface, opaque side-local cursors, query/threshold/page fingerprints, total order with a tie-break, bounded export equivalence instead of an unbounded dump, and k-anonymous aggregate receipts. |
| [0010 - Phase 3 object encryption and capability contract](decisions/0010-phase3-object-encryption-and-capability-contract.md) | What a production encryption boundary must be before a key provider, cipher or backend is chosen: a key authority rather than a key store, one authority-generated DEK per object generation, AAD binding over the published identity layer, dedupe at the identity layer instead of convergent ciphertext, a reader that authenticates before it exposes, probed rather than assumed backend guarantees, IAM-enforced delete denial, re-wrap rotation with crypto-erasure revocation, versioned sealed metadata, and twelve gates. Activates nothing. |
| [phase3-object-encryption-contract.md](phase3-object-encryption-contract.md) | The operational specification behind 0010: the `KeyAuthority` protocol and its capability record, the key hierarchy and nonce-uniqueness obligation, the dedup-versus-leakage tradeoff and its residuals, the frame/trailer format and the attacks each AAD field stops, the reader constraint and its two consumer shapes, the `hear.objectstore.sealedmeta.v2` clear/sealed split and its v1→v2 story, the thirteen capability probes, the credential matrix, rotation/revocation procedures, the closed failure vocabulary, and what may never be emitted. |
| [phase6-list-read-comparison.md](phase6-list-read-comparison.md) | The operational specification behind 0009: surfaces and grains, pin kinds, ordering and pagination semantics, absence/late/deleted/retention cases, bounded export equivalence, aggregate privacy rules, sampling and cost caps, the twenty-three-member classification, the staged `hear.readcompare.receipt.v1`, 32 fault-injection cases, legacy-authoritative rollout/rollback and the nine promotion gates. |

## System and subsystem references

| Document | What it covers |
|---|---|
| [architecture.md](architecture.md) | Current system overview. |
| [acoustic-stack.md](acoustic-stack.md) | The acoustic processing stack end to end. |
| [clip-pipeline.md](clip-pipeline.md) | Clip capture, drain, indexing, and retention. |
| [timing.md](timing.md) | Clocking, boot-relative time, and trusted UTC. |
| [uplink.md](uplink.md) | Node uplink transports and envelopes. |
| [esp32s3-lora-node.md](esp32s3-lora-node.md) | ESP32-S3 LoRa node design. |
| [node-hardware.md](node-hardware.md) | Node hardware reference. |
| [faketec-pin-budget.md](faketec-pin-budget.md) | Board pin budget. |
| [l86-reference.md](l86-reference.md) | L86 GNSS module reference. |
| [hear-latency-calibration-runbook.md](hear-latency-calibration-runbook.md) | Latency calibration procedure. |
| [pool-backup-restore.md](pool-backup-restore.md) | The backup and restore design for PVC `dama/hear-pool` and its explicit G0 acceptance test: measured storage facts (including that the node's ext4 root is a VHDX on a 95 %-full Windows disk), why no snapshot primitive exists here, generational encrypted archives to a second physical disk, per-class consistency rules for append-only JSONL and a live-WAL SQLite database, key custody, retention, and the restore-isolation boundary. Phase 3 import is blocked until its G0 passes. |
| [bridge-durable-soak.md](bridge-durable-soak.md) | Durable-outbox soak runbook for `hear-mqtt-bridge`: what `tools/bridge_soak_evidence.py` records, the T+24h/7d/14d pass/fail criteria, and the read-only boundary it enforces. |

## Operations, policy, and cross-cutting design

| Document | What it defines |
|---|---|
| [analytics.md](analytics.md) | The analytics layer above geometry and detectors: event model, modality naming rules, correlation, tracks, geofences, and alerts with provenance back to raw evidence. |
| [cost-capacity.md](cost-capacity.md) | The fleet sizing model: uplink, storage, and clip/audio cost formulas with stated default variables, to be recalculated per deployment. |
| [data-governance.md](data-governance.md) | Self-hosted acoustic data governance: data classes, recording indicators, retention, access, export, and audit controls for captured and derived data. |
| [resilience.md](resilience.md) | Failure-mode design: the invariants that must survive, degraded-mode behaviour per dependency, and how operators learn about failure without reading a stack trace. |
| [durable-outbox-failure-drill-runbook.md](durable-outbox-failure-drill-runbook.md) | Maintenance-window procedure for the `hear-mqtt-bridge` durable-outbox induced-failure drill: gate, prechecks, backup, scoped fault injection, expected observations, abort triggers, recovery order, evidence receipt, and pass criteria. |
| [durable-outbox-failure-drill-evidence.md](durable-outbox-failure-drill-evidence.md) | The offline half of that drill: the evidence-bundle schema, the exact abort boundary as constants (900 s, 3 restarts, 500 MiB, 256-row backlog), the blocking execution gate, blast-radius screening of the planned commands, and the post-drill grading `tools/bridge_drill_evidence.py` performs. Reads local files only; runs nothing. |
| [phase7-redis-census-tool.md](phase7-redis-census-tool.md) | The dry-run, read-only `dama:hear:*` census tool: its command allow-list and prefix confinement, the consent gate and capture-time redaction for a client census, TTL/eviction/boundedness grading under `allkeys-lru`, staged receipts, and the stage-ordering gate. Live execution against the shared `infra/audit-redis` is a separately authorized operator task and has not been run. |
| [ota-release-credentials.md](ota-release-credentials.md) | The tokenless-release incident, the firmware secret audit, and the NVS credential contract that keeps published images secret-free without making a node OTA-unrecoverable. |
| [release-v0.1.6-readiness.md](release-v0.1.6-readiness.md) | The pre-cut gate for v0.1.6: required merged prerequisites, artifact variants and the `fs_acquisition_hz: 48000` manifest check, the unprovisioned-vs-fleet-ready public asset contract, operator-selected admin-token provisioning, pre-cut checks, per-node rollout order and no-go gates, the deliberate `nyquist` rollback receipt, and failure/rollback procedures. Not executed. |
| [rankine-usb-recovery.md](rankine-usb-recovery.md) | The `rankine` USB recovery and data-preservation package: why recovery is physical-only, the data pull and receipt taken before anything is written, a custody handoff that moves no secret value, the single `enroll.py` USB session that writes image and NVS record, token-free enrollment acceptance, post-recovery validation of 48 kHz acquisition / heartbeat / fail-closed admin auth / streaming and drain, no-go and stop conditions, the honest absence of a rollback, and the Phase 2 soak baseline consequence recorded as an open operator decision. Not executed; `rankine` was not contacted. |
| [fleet-release-rollout-sequence.md](fleet-release-rollout-sequence.md) | The dependency-aware, non-executing sequence that follows the v0.1.6 readiness gate: measured state after the cut and rollout, the admin-token decision restated as a migration, the provenance debt on the published v0.1.6 and how the next tag closes it, NVS provisioning order and token-free verification, canary/mixed-version staging, the unexercised `nyquist` failback receipt, `rankine` USB recovery, `gold` quad ring sizing, backups, stop/rollback gates, and the soak interaction. Not executed. |
| [gold-release-flash-acceptance.md](gold-release-flash-acceptance.md) | The `gold`-only recovery and release acceptance package for a future flash with a raw-ring tag: the SBOM/attestation release gate, the credential-safe USB/NVS provisioning handoff verified without disclosing a token, quad-PSRAM verification, raw-ring/detection-ring/heap-margin thresholds with the 2 MiB tier arithmetic, `/audio` PCM streaming validation, watchdog and telemetry health checks, abort and rollback conditions, and the statement that the flash is a fleet-composition change to be graded against the Phase 2 soak baseline. Not executed. |

## Field findings and calibration records

Dated, point-in-time evidence; superseded only by a later dated record.

* [findings-2026-09-05.md](findings-2026-09-05.md), [findings-2026-09-06.md](findings-2026-09-06.md)
* [crack-blast-2026-09-05.md](crack-blast-2026-09-05.md)
* [field-pull-2026-09-08.md](field-pull-2026-09-08.md)
* [localisation-2026-09-08.md](localisation-2026-09-08.md)
* [placement-weekend-2026-09-08.md](placement-weekend-2026-09-08.md)
* [clip-calibration-2026-09-10.md](clip-calibration-2026-09-10.md)
* [findings-clap-calibration-2026-09-14.md](findings-clap-calibration-2026-09-14.md)
* [validation-full-captures.md](validation-full-captures.md)

# hear-ml-gpu: the CUDA wheels the hear-embed / perch_v2 lane installs into /pool/pylib-perch at
# every run today (deploy/k8s/hear-embed.yaml), moved into the image.
#
# ⚠️TensorFlow ITSELF IS NOT HERE. It comes from the base image, tensorflow/tensorflow:2.20.0-gpu,
# pinned by digest in deploy/images/base-images.txt. Restating it as a pip requirement would
# resolve a second, different TensorFlow on top of the vendored one.
#
# ⚠️These twelve wheels are a matched CUDA 12.9 / cuDNN 9.26 set, which is why the live manifest
# installs them with --no-deps and marks the directory with a `.cuda-12.9-cudnn-9.26` sentinel.
# The lock replaces the sentinel: the layer either has the resolved set or the build failed.
#
# Resolved to requirements/lock/ml-gpu.txt.
nvidia-cublas-cu12==12.9.2.10
nvidia-cuda-cupti-cu12==12.9.79
nvidia-cuda-nvcc-cu12==12.9.86
nvidia-cuda-nvrtc-cu12==12.9.86
nvidia-cuda-runtime-cu12==12.9.79
nvidia-cudnn-cu12==9.26.0.51
nvidia-cufft-cu12==11.4.1.4
nvidia-curand-cu12==10.3.10.19
nvidia-cusolver-cu12==11.7.5.82
nvidia-cusparse-cu12==12.5.10.65
nvidia-nccl-cu12==2.31.2
nvidia-nvjitlink-cu12==12.9.86
